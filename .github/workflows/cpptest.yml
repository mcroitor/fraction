name: C++ Test

on: 
  schedule:
    - cron:  '20 2 * * *'
  pull_request:
    branches:
      - test

jobs:
  test_app:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: branch name
      run: echo running on branch ${GITHUB_REF##*/}
    - name: rebase
      run: git rebase dev
    - name: test library
      run: make test